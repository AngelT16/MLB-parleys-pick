{
  "name": "mlb-parleys-pick",
  "version": "1.0.0",
  "private": true,
  "description": "MLB Parleys Pick - data-driven MLB parlay generator (8-10 legs) with probability, edge and correlation analysis.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "test": "npm run test:run --prefix server",
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
